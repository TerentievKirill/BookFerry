name: External E2E

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  external-e2e:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          npm install -g allure-commandline

      - name: Build BookFerry
        run: docker build -t bookferry-test .

      - name: Start BookFerry
        run: |
          docker run -d \
            --name bookferry-test \
            -p 8000:8000 \
            -e DB_NAME=/app/tests/data/bookferry_test.db \
            -e CATALOG_DB_NAME=/app/tests/data/catalog_test.db \
            bookferry-test

          sleep 2

      - name: Run smoke tests
        id: smoke
        continue-on-error: true
        run: pytest tests -v -s -m "not e2e" --alluredir=allure-results

      - name: Run external E2E tests
        id: e2e
        continue-on-error: true
        run: pytest tests/test_external_e2e.py -v -s -m e2e --alluredir=allure-results

      - name: Restore Allure history
        if: always()
        run: |
          git fetch origin gh-pages:refs/remotes/origin/gh-pages || true
          if git show-ref --verify --quiet refs/remotes/origin/gh-pages; then
            mkdir -p previous-report
            git archive origin/gh-pages | tar -x -C previous-report
            if [ -d previous-report/history ]; then
              cp -r previous-report/history allure-results/history
            fi
          fi

      - name: Generate Allure report
        if: always()
        run: |
          allure generate allure-results --clean -o allure-report
          touch allure-report/.nojekyll

      - name: Publish Allure report
        if: always()
        run: |
          cp -a allure-report /tmp/allure-report

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout --orphan gh-pages-build
          git rm -rf . || true
          git clean -fdx
          cp -a /tmp/allure-report/. .
          git add -A
          git commit -m "update Allure report"
          git push --force origin HEAD:gh-pages

      - name: Show container logs on failure
        if: steps.smoke.outcome == 'failure' || steps.e2e.outcome == 'failure'
        run: docker logs bookferry-test

      - name: Fail workflow if tests failed
        if: steps.smoke.outcome == 'failure' || steps.e2e.outcome == 'failure'
        run: exit 1
